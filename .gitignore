# User data, not source. The repo ships projects.conf.example instead.
projects.conf

# Internal design docs / agent scratch — not part of the public source tree.
docs/
docs/superpowers/

# Build artifacts
dist/
*.deb
*.rpm
*.tar.gz
*.tar.gz.sha256
SHA256SUMS

# Logs / state if anyone runs from a checkout
*.log

# Editor crud
.vscode/
.idea/
*.swp
*~
.DS_Store

# Claude Code agent scratch (worktrees, local settings)
.claude/
